spring:
  application:
    name: service-auth
  cloud:
    config:
      uri: http://localhost:8090
  profiles:
    active: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8082